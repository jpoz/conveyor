from setuptools import setup, find_packages

setup(
    name='protojob',
    version='0.1',
    packages=find_packages(),
    install_requires=[
        # list your package dependencies here
    ],
    entry_points={
        # specify command-line scripts here if any
    }
)
